
server.port=8086

# === Thymeleaf (rất quan trọng để thấy giao diện đẹp ngay khi sửa HTML) ===
spring.thymeleaf.cache=false
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# === Static resources (ảnh, css, js) ===
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=0

# === DevTools – tự restart + livereload cực nhanh khi sửa code/HTML ===
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# === Logging (dễ debug) ===
logging.level.org.springframework.web=INFO
logging.level.org.thymeleaf=INFO

# === Sau này làm backend thì bỏ comment mấy dòng dưới (hiện tại chưa cần) ===
# spring.datasource.url=jdbc:mysql://localhost:3306/rentcar_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
# spring.datasource.username=root
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true